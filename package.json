{
  "name": "sse-query-invalidator-workspace",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "install:all": "pnpm install",
    "build": "pnpm --filter restale-kit run build",
    "dev:client": "pnpm --filter restale-kit-example-client run dev",
    "dev:server": "pnpm --filter restale-kit-example-server run dev"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
